I need you to fully FIX and UPGRADE the Challenge Claim system on Jango so it works perfectly and feels like a AAA game reward experience.

Right now it is broken and unpolished:
- Error toast still appears (“Could not claim reward”)
- XP does not clearly flow into progression
- No satisfying feedback (no animation, no sound)
- Feels like a bug, not a reward

This needs to be completely rebuilt and polished.

-----------------------------------
CORE GOAL
-----------------------------------
When a user clicks “Claim”, it should:
- Successfully award XP
- Update progression instantly
- Remove claim state
- Play sound + animation
- NEVER show an error unless something actually fails

This should feel like a premium game reward system.

-----------------------------------
STEP 1: FIX THE BUG (MANDATORY)
-----------------------------------

Fully debug the claim flow end-to-end:

Check:
- Frontend click handler
- API request
- Backend claim validation
- XP reward application
- Challenge claimed state
- State refresh / cache invalidation
- Toast logic

SPECIFIC ISSUES TO FIX:

1. Duplicate requests
- Prevent double clicks
- Disable button immediately
- Add loading state
- Ensure only ONE request fires

2. False error toast
- Do NOT show “Could not claim reward” if the reward succeeded
- Fix response handling so success ≠ error
- Remove any generic catch() that triggers error after success

3. State mismatch
- After claim:
  - Challenge must become “Claimed”
  - Ready-to-claim count must update
  - UI must refresh immediately

4. Partial success bug
- Ensure this is NOT happening:
  - First request succeeds
  - Second request fails
  - Error toast shows after success

Fix this at the root.

-----------------------------------
STEP 2: XP PROGRESSION (REAL-TIME)
-----------------------------------

When XP is claimed:

- Add XP instantly to user
- Update XP bar immediately
- Update level immediately
- Update:
  - XP value
  - progress bar fill
  - level badge
  - next reward text

If user levels up:
- Trigger level-up state instantly

-----------------------------------
STEP 3: CLEAN UI STATE
-----------------------------------

After successful claim:

- Replace button with:
  “Claimed” or completed state
- Fade or dim completed challenge
- Reduce “Ready to Claim” count
- Remove any error messages

-----------------------------------
STEP 4: ADD AAA SOUND SYSTEM
-----------------------------------

Add:

1. CLAIM SOUND
- Plays once when XP is granted
- Clean, satisfying (reward/coin feel)
- Not loud or annoying

2. LEVEL-UP SOUND (only if level up)
- Slightly bigger, more exciting

Rules:
- No duplicate sounds
- Only trigger on success
- Must work on mobile + desktop

-----------------------------------
STEP 5: ADD AAA ANIMATIONS
-----------------------------------

Make claiming XP feel rewarding.

1. BUTTON ANIMATION
- On click: slight scale down → bounce back
- Glow effect (yellow/orange)
- Show loading spinner briefly

2. XP POPUP TEXT
Show floating text:
+50 XP / +75 XP / +100 XP

Animation:
- Pops up from button
- Moves upward
- Fades out smoothly

3. XP TRANSFER ANIMATION (IMPORTANT)
Make XP visually go into the XP bar.

Flow:
- Particles/glow originate from claim button
- Travel toward XP progress bar
- XP bar fills smoothly when particles land

4. XP BAR ANIMATION
- Smooth fill animation (not instant jump)
- Slight glow pulse when updated

5. LEVEL-UP ANIMATION (IF APPLICABLE)
If level increases:
- Show:
  LEVEL UP
  Level X → Level Y
- Glow around profile / level badge
- Slight screen pulse

-----------------------------------
STEP 6: FINAL CLAIM FLOW (TARGET)
-----------------------------------

The final experience should be:

User clicks Claim  
→ Button animates  
→ Loading state briefly  
→ Sound plays  
→ +XP popup appears  
→ XP particles move to XP bar  
→ XP bar fills smoothly  
→ Level updates (if needed)  
→ Challenge becomes “Claimed”  
→ Ready count updates  
→ No error toast  

-----------------------------------
STEP 7: ERROR HANDLING
-----------------------------------

Only show error IF:
- Backend fails
- Network fails
- Reward is actually invalid

If error:
- Show clean toast
- Do NOT update UI incorrectly

-----------------------------------
IMPORTANT
-----------------------------------

- Do NOT just hide the error toast
- Do NOT patch over the issue
- Find and fix the root cause

-----------------------------------
FINAL RESULT
-----------------------------------

This should feel like:
- A real game reward system
- Smooth, fast, satisfying
- Visually clear and bug-free

Claiming XP should feel GOOD — not confusing or broken.